{
  "TODO": {
    "description": "Copy this template, fill in the values, add/remove fields as needed, and remove the entire TODO field.",
    "save_instructions": "Save as a .json file in the interface/directory."
  },
  "assay_type": "required",
  "target_phenotype": "required",
  "organism": "required",
  "cell_line_or_system": "required",
  "perturbation": {
    "type": "required",
    "library_or_reagent": "required"
  },
  "readout": {
    "measurement": "required",
    "instrument_or_platform": "required",
    "primary_metric": "required"
  },
  "clustering": {
    "method": "required",
    "parameters": {
      "resolution": "adjustable",
      "k_neighbors": "adjustable"
    }
  },
  "controls": {
    "negative_controls": "required",
    "positive_controls": "required"
  },
  "provenance": {
    "dataset_name": "required",
    "citation": "required",
    "data_source": "required"
  },
  "notes": "optional",
  "phenotype_readout": {
    "features_description": "TODO: what the up/down feature lists are and what cutoff produced them (omit this block if your table has no feature columns)",
    "strength_description": "TODO: what the perturbation-strength metric measures and how it was computed vs controls (omit if no strength column)"
  }
}